## 1. Implementacion

- [x] 1.1 inicia el servidor en puerto libre (default 3100), sirve la SPA en `/` y loguea `Dashboard en http://127.0.0.1:<port>`
- [x] 1.2 abre el navegador del sistema a la URL efectiva (`open`/`xdg-open`/`start` según OS) sin bloquear el proceso
- [x] 1.3 documenta uso, flags (`--open`, `--port`, `--host`), ejemplos y que es ESM estricto, en español

## 2. Verificacion

- [x] 2.1 Test del criterio 1: inicia el servidor en puerto libre (default 3100), sirve la SPA en `/` y loguea `Dashboard en http://127.0.0.1:<port>`
- [x] 2.2 Test del criterio 2: abre el navegador del sistema a la URL efectiva (`open`/`xdg-open`/`start` según OS) sin bloquear el proceso
- [x] 2.3 Test del criterio 3: documenta uso, flags (`--open`, `--port`, `--host`), ejemplos y que es ESM estricto, en español
