## 1. Implementacion

- [ ] 1.1 se abre un panel lateral contextual (drawer) mostrando el markdown fuente, requisitos funcionales vinculados, criterios Given/When/Then y estado git
- [ ] 1.2 se despliega el terminal drawer con xterm.js, se ejecuta el comando `build` asociado mediante streaming en tiempo real y el nodo en el canvas entra en estado visual animado `building`
- [ ] 1.3 el nodo transiciona automáticamente a estado verde `completed` en el lienzo con un micro-feedback visual

## 2. Verificacion

- [ ] 2.1 Test del criterio 1: se abre un panel lateral contextual (drawer) mostrando el markdown fuente, requisitos funcionales vinculados, criterios Given/When/Then y estado git
- [ ] 2.2 Test del criterio 2: se despliega el terminal drawer con xterm.js, se ejecuta el comando `build` asociado mediante streaming en tiempo real y el nodo en el canvas entra en estado visual animado `building`
- [ ] 2.3 Test del criterio 3: el nodo transiciona automáticamente a estado verde `completed` en el lienzo con un micro-feedback visual
