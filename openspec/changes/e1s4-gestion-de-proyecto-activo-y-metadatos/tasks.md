## 1. Implementacion

- [ ] 1.1 `projects.json` actualiza `activeProjectId = proj-2` y `lastActive` de `proj-2` a `now()` ISO-8601 con escritura atómica
- [ ] 1.2 `activeProjectId` persiste el último valor guardado

## 2. Verificacion

- [ ] 2.1 Test del criterio 1: `projects.json` actualiza `activeProjectId = proj-2` y `lastActive` de `proj-2` a `now()` ISO-8601 con escritura atómica
- [ ] 2.2 Test del criterio 2: `activeProjectId` persiste el último valor guardado
