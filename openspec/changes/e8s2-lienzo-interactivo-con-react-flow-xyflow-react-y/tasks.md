## 1. Implementacion

- [ ] 1.1 se renderiza un canvas interactivo React Flow con nodos customizados tipados: `EpicNode`, `StoryNode`, `SpecNode`, `ContractNode` y `WaveNode`
- [ ] 1.2 el motor calcula la distribución jerárquica libre de colisiones (algoritmo DAG por niveles) respetando el flujo de izquierda a derecha (o arriba a abajo)
- [ ] 1.3 el rendimiento se mantiene a 60 fps estables sin retrasos ni degradación visual
- [ ] 1.4 el nodo actualiza su estilo visual, borde de color e indicador de estado de forma reactiva

## 2. Verificacion

- [ ] 2.1 Test del criterio 1: se renderiza un canvas interactivo React Flow con nodos customizados tipados: `EpicNode`, `StoryNode`, `SpecNode`, `ContractNode` y `WaveNode`
- [ ] 2.2 Test del criterio 2: el motor calcula la distribución jerárquica libre de colisiones (algoritmo DAG por niveles) respetando el flujo de izquierda a derecha (o arriba a abajo)
- [ ] 2.3 Test del criterio 3: el rendimiento se mantiene a 60 fps estables sin retrasos ni degradación visual
- [ ] 2.4 Test del criterio 4: el nodo actualiza su estilo visual, borde de color e indicador de estado de forma reactiva
