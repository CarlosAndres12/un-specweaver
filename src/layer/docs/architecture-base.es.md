# Arquitectura base

> Plantilla generada por `un-specweaver init`. Reemplaza el contenido por la arquitectura real
> de tu organizacion. Este archivo se carga como contexto en toda fase de diseno y
> construccion, asi que lo que este aqui manda sobre lo que el agente asuma por defecto.
>
> `un-specweaver init` nunca sobreescribe este archivo si ya existe.

## Stack por defecto

<!-- Lenguajes, runtimes y versiones que la organizacion soporta. Se explicito con las
     versiones: "Node 22 LTS", no "Node reciente". -->

## Estructura de proyecto

<!-- Como se organiza un repo aqui. Que carpetas existen y que va en cada una. -->

## Fronteras y capas

<!-- Que puede importar que. Donde vive la logica de dominio. Que esta prohibido
     acoplar. Esta seccion es la que mas evita reescrituras. -->

## Persistencia

<!-- Motores, patrones de acceso, migraciones, convenciones de nombres. -->

## Autenticacion y autorizacion

<!-- El mecanismo estandar. Que NO se implementa a mano nunca. -->

## Manejo de errores y observabilidad

<!-- Formato de logs, correlation ids, que se instrumenta, donde se ve. -->

## Testing

<!-- Que niveles son obligatorios, que herramientas, que cobertura se exige,
     que se considera un test valido. -->

## Seguridad

<!-- Manejo de secretos, dependencias permitidas, revisiones obligatorias. -->

## Decisiones cerradas

<!-- Cosas que ya se decidieron y no se rediscuten en cada proyecto.
     Formato: decision — por que — cuando se tomaria de nuevo. -->

## Fuera de alcance

<!-- Tecnologias o patrones explicitamente rechazados, con la razon.
     Sin esto, el agente los va a proponer una y otra vez. -->
