# Base architecture

> Template generated by `un-specweaver init`. Replace the content with your organization's real
> architecture. This file is loaded as context in every design and build phase, so what is here
> overrides what the agent would otherwise assume by default.
>
> `un-specweaver init` never overwrites this file once it exists.

## Default stack

<!-- Languages, runtimes and versions the organization supports. Be explicit about versions:
     "Node 22 LTS", not "recent Node". -->

## Project structure

<!-- How a repo is laid out here. Which folders exist and what goes in each. -->

## Boundaries and layers

<!-- What may import what. Where domain logic lives. What must never be coupled.
     This section prevents more rewrites than any other. -->

## Persistence

<!-- Engines, access patterns, migrations, naming conventions. -->

## Authentication and authorization

<!-- The standard mechanism. What is NEVER hand-rolled. -->

## Error handling and observability

<!-- Log format, correlation ids, what gets instrumented, where it is viewed. -->

## Testing

<!-- Which levels are mandatory, which tools, what coverage is required,
     what counts as a valid test. -->

## Security

<!-- Secret handling, allowed dependencies, mandatory reviews. -->

## Settled decisions

<!-- Things already decided that are not re-litigated per project.
     Format: decision — why — when it would be revisited. -->

## Out of scope

<!-- Technologies or patterns explicitly rejected, with the reason.
     Without this, the agent will propose them again and again. -->
